# Electron Host

This template is copied into `dist/packager/work/<slug>/electron-host` by the
packager scripts. The source NexusRealtime project is copied into `app/` after
it has been normalized as a static web bundle.
